<div class="post-preview">
  <h3 class="post-title">
    <a href="/post/{{ post.id }}">{{ post.title }}</a>
  </h3>
  <p class="post-text">{{ post.content }}</p>
  <p class="post-meta">Posted by {{ post.user.username }} on {{ post.created_at }}</p>
</div>